
names(sort(-table(by2olak_data_proper_status$road_status_id)))[1]
names(sort(-table(by2olak_data_proper_status$report_status_id)))[1]
names(sort(-table(by2olak_data_proper_status$road_update_hours_ago))[1]
names(sort(-table(by2olak_data_proper_status$report_update_hours_ago))[1]

```{r}
pie(table(by2olak_data$report_status_id))
pie(table(by2olak_data$road_status_id))
```

ggplot(data=by2olak_data, aes(by2olak_data$road_status_id)) + geom_histogram()
ggplot(data=by2olak_data, aes(by2olak_data$report_status_id)) + geom_histogram()


barplot(table(by2olak_data['report_status_id']), main = "Report Status Values Distribution")
barplot(table(by2olak_data['road_status_id']), main = "Road Status Values Distribution")

# ggplot(data = melt(subset(by2olak_data, select=-c(day, actual_day, crawl_date, road.header, road.start, road.destination, gps, anonymous,road_name,report_username, report_comment))), aes(x=variable, y=value)) + geom_boxplot(aes(fill=variable))

qplot(road_route_id, road_status_id, data = by2olak_data, col=actual_day)

Tables od columns!!


 mydata[order(mydata$B),]


Average status for each day

Comments:
bots
anonymous
users


frequeny of active users!
users -> reports

occurences of each status of road, report

Confidence Interval of a Mean in R


----------------------------------------------------
Comments on mean, median ,.........
Explain results of the graphs and plots
Comments on the data visualization 
